# user/questions.js is each user's personal problem bank — git-ignored so local edits
# never show up as uncommitted changes. user/questions.template.js is the tracked
# starter template. Any other files added to user/ are also not tracked.
user/*
!user/questions.template.js

# Common system files
.DS_Store
Thumbs.db
